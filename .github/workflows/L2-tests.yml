name: L2-tests

on:
  workflow_call:
    inputs:
      caller_source:
        description: "Specifies the source type (e.g., local or test framework) for the workflow."
        required: true
        type: string

env:
  BUILD_TYPE: Debug
  THUNDER_REF: "R4.4.1"
  INTERFACES_REF: "develop"
  NETWORKMANAGER_REF: "cea0da05b03de770e4ce967f0446f28885fc6219"
  AUTOMATICS_UNAME: ${{ secrets.AUTOMATICS_UNAME}}
  AUTOMATICS_PASSCODE: ${{ secrets. AUTOMATICS_PASSCODE}}

jobs:
  L2-tests:
    name: Build and run L2 tests
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        compiler: [ gcc, clang ]
        coverage: [ with-coverage, without-coverage ]
        exclude:
          - compiler: clang
            coverage: with-coverage
          - compiler: clang
            coverage: without-coverage
          - compiler: gcc
            coverage: without-coverage

    steps:
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - run: pip install jsonref

      - name: ACK External Trigger
        run: |
           echo "Message: External Trigger Received for L2 Tests"
           echo "Trigger Source: ${{ inputs.caller_source }}"

      - name: Set up CMake
        uses: jwlawson/actions-setup-cmake@65b272e1213f99771fbba0c237e0b3312270e0c9
        with:
          cmake-version: '3.16.x'

      - name: Install packages
        run: >
          sudo apt update
          &&
          sudo apt install -y libsqlite3-dev libcurl4-openssl-dev valgrind lcov clang libsystemd-dev libboost-all-dev libwebsocketpp-dev meson libcunit1 libcunit1-dev curl protobuf-compiler-grpc libgrpc-dev libgrpc++-dev
         # &&
         # apt-get install -y coreutils mtools dosfstools

      - name: Install GStreamer
        run: |
           sudo apt update
           sudo apt install -y libunwind-dev libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev

      - name: Build trevor-base64
        run: |
            if [ ! -d "trower-base64" ]; then
            git clone https://github.com/xmidt-org/trower-base64.git
            fi
            cd trower-base64
            meson setup --warnlevel 3 --werror build
            ninja -C build
            sudo ninja -C build install

      - name: Checkout Thunder
        uses: actions/checkout@v3
        with:
          repository: rdkcentral/Thunder
          path: Thunder
          ref: ${{env.THUNDER_REF}}

      - name: Checkout ThunderTools
        uses: actions/checkout@v3
        with:
          repository: rdkcentral/ThunderTools
          path: ThunderTools
          ref: R4.4.3

      - name: Checkout entservices-appgateway
        if: ${{ inputs.caller_source == 'local' }}
        uses: actions/checkout@v3
        with:
          path: entservices-appgateway

      - name: Checkout entservices-appgateway-testframework
        if: ${{ inputs.caller_source == 'testframework' }}
        uses: actions/checkout@v3
        with:
          repository: rdkcentral/entservices-appgateway
          path: entservices-appgateway
          ref: develop

      - name: Checkout entservices-deviceanddisplay
        uses: actions/checkout@v3
        with:
          repository: rdkcentral/entservices-deviceanddisplay
          path: entservices-deviceanddisplay
          ref: develop
         
      - name: Checkout entservices-testframework
        uses: actions/checkout@v3
        with:
          repository: rdkcentral/entservices-testframework
          path: entservices-testframework
          ref: 1.0.1

      - name: Checkout googletest
        if: steps.cache.outputs.cache-hit != 'true'
        uses: actions/checkout@v3
        with:
          repository: google/googletest
          path: googletest
          ref: v1.15.0

      - name: Apply patches ThunderTools
        run: |  
          cd $GITHUB_WORKSPACE/ThunderTools
          patch -p1 < $GITHUB_WORKSPACE/entservices-testframework/patches/00010-R4.4-Add-support-for-project-dir.patch
          cd -

      - name: Build ThunderTools
        run: >
          cmake
          -S "$GITHUB_WORKSPACE/ThunderTools"
          -B build/ThunderTools
          -DEXCEPTIONS_ENABLE=ON
          -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/install/usr"
          -DCMAKE_MODULE_PATH="$GITHUB_WORKSPACE/install/tools/cmake"
          -DGENERIC_CMAKE_MODULE_PATH="$GITHUB_WORKSPACE/install/tools/cmake"
          &&
          cmake --build build/ThunderTools -j8
          &&
          cmake --install build/ThunderTools

      - name: Apply patches Thunder
        run: |
          cd $GITHUB_WORKSPACE/Thunder
          patch -p1 < $GITHUB_WORKSPACE/entservices-testframework/patches/Use_Legact_Alt_Based_On_ThunderTools_R4.4.3.patch
          patch -p1 < $GITHUB_WORKSPACE/entservices-testframework/patches/error_code_R4_4.patch
          patch -p1 < $GITHUB_WORKSPACE/entservices-testframework/patches/1004-Add-support-for-project-dir.patch
          patch -p1 < $GITHUB_WORKSPACE/entservices-testframework/patches/RDKEMW-733-Add-ENTOS-IDS.patch
          patch -p1 < $GITHUB_WORKSPACE/entservices-testframework/patches/Jsonrpc_dynamic_error_handling.patch
          patch -p1 < $GITHUB_WORKSPACE/entservices-testframework/patches/Increase_Timout_For_L2Tests_Plugin.patch
          cd -
      - name: Build Thunder
        run: >
          cmake
          -S "$GITHUB_WORKSPACE/Thunder"
          -B build/Thunder
          -DMESSAGING=ON
          -DHIDE_NON_EXTERNAL_SYMBOLS=OFF
          -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/install/usr"
          -DCMAKE_MODULE_PATH="$GITHUB_WORKSPACE/install/tools/cmake"
          -DBUILD_TYPE=${{env.BUILD_TYPE}}
          -DBINDING=127.0.0.1
          -DPORT=9998
          -DEXCEPTIONS_ENABLE=ON
          &&
          cmake --build build/Thunder -j8
          &&
          cmake --install build/Thunder

      - name: Checkout entservices-apis
        uses: actions/checkout@v3
        with:
          repository: rdkcentral/entservices-apis
          path: entservices-apis
          ref: ${{env.INTERFACES_REF}}
          run: rm -rf $GITHUB_WORKSPACE/entservices-apis/jsonrpc/DTV.json

      - name: Apply patches entservices-apis
        run: |
          cd $GITHUB_WORKSPACE/entservices-apis
          patch -p1 < $GITHUB_WORKSPACE/entservices-testframework/patches/RDKEMW-1007.patch
          patch -p1 < $GITHUB_WORKSPACE/entservices-testframework/patches/add-l2tests-interface.patch
          cd -    

      - name: Build entservices-apis
        run: >
          cmake
          -S "$GITHUB_WORKSPACE/entservices-apis"
          -B build/entservices-apis
          -DEXCEPTIONS_ENABLE=ON
          -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/install/usr"
          -DCMAKE_MODULE_PATH="$GITHUB_WORKSPACE/install/tools/cmake"
          &&
          cmake --build build/entservices-apis -j8
          &&
          cmake --install build/entservices-apis

      - name: Generate external headers
        # Empty headers to mute errors
        run: >
          cd "$GITHUB_WORKSPACE/entservices-testframework/Tests/"
          &&
          mkdir -p
          headers
          headers/rdk/ds
          headers/rdk/iarmbus
          headers/rdk/iarmmgrs-hal
          headers/systemservices
          headers/systemservices/proc
          headers/libusb
          headers/Dobby
          headers/Dobby/Public/Dobby
          headers/Dobby/IpcService
          &&
          cd headers
          &&
          touch
          rdk/ds/host.hpp
          rdk/ds/videoOutputPort.hpp
          rdk/ds/videoOutputPortType.hpp
          rdk/ds/videoOutputPortConfig.hpp
          rdk/ds/videoResolution.hpp
          rdk/ds/audioOutputPort.hpp
          rdk/ds/audioOutputPortType.hpp
          rdk/ds/sleepMode.hpp
          rdk/ds/frontPanelConfig.hpp
          rdk/ds/frontPanelTextDisplay.hpp
          rdk/ds/hdmiIn.hpp
          rdk/ds/compositeIn.hpp
          rdk/ds/audioOutputPortConfig.hpp
          rdk/ds/exception.hpp
          rdk/ds/dsError.h
          rdk/ds/dsMgr.h
          rdk/ds/manager.hpp
          rdk/ds/dsTypes.h
          rdk/ds/dsUtl.h
          rdk/ds/audioOutputPortType.hpp
          rdk/ds/audioOutputPortConfig.hpp
          rdk/ds/pixelResolution.hpp
          rdk/iarmbus/libIARM.h
          rdk/iarmbus/libIBus.h
          rdk/iarmbus/libIBusDaemon.h
          rdk/iarmmgrs-hal/mfrMgr.h
          rdk/iarmmgrs-hal/pwrMgr.h
          rdk/iarmmgrs-hal/sysMgr.h
          rdk/iarmbus/iarmUtil.h
          rfcapi.h
          rbus.h
          libudev.h
          libusb/libusb.h
          systemservices/proc/readproc.h
          systemservices/secure_wrapper.h
          systemaudioplatform.h
          maintenanceMGR.h
          pkg.h
          edid-parser.hpp
          btmgr.h
          tvError.h
          tvTypes.h
          tvTypesODM.h
          tvSettings.h
          tvSettingsExtODM.h
          tvSettingsODM.h
          tvTypesODM.h
          tr181api.h
          list.hpp
          dsDisplay.h
          rdk/ds/AudioStereoMode.hpp
          rdk/ds/VideoDFC.hpp
          dsRpc.h
          Dobby/DobbyProtocol.h
          Dobby/DobbyProxy.h
          Dobby/Public/Dobby/IDobbyProxy.h
          Dobby/IpcService/IpcFactory.h
          &&
          cp -r /usr/include/gstreamer-1.0/gst /usr/include/glib-2.0/* /usr/lib/x86_64-linux-gnu/glib-2.0/include/* /usr/local/include/trower-base64/base64.h /usr/include/libdrm/drm.h /usr/include/libdrm/drm_mode.h /usr/include/xf86drm.h .

      - name: Set clang toolchain
        if: ${{ matrix.compiler == 'clang' }}
        run: echo "TOOLCHAIN_FILE=$GITHUB_WORKSPACE/entservices-testframework/Tests/clang.cmake" >> $GITHUB_ENV

      - name: Set gcc/with-coverage toolchain
        if: ${{ matrix.compiler == 'gcc' && matrix.coverage == 'with-coverage' && !env.ACT }}
        run: echo "TOOLCHAIN_FILE=$GITHUB_WORKSPACE/entservices-testframework/Tests/gcc-with-coverage.cmake" >> $GITHUB_ENV

      - name: Build googletest
        if: steps.cache.outputs.cache-hit != 'true'
        run: >
          cmake -G Ninja
          -S "$GITHUB_WORKSPACE/googletest"
          -B build/googletest
          -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/install/usr"
          -DCMAKE_MODULE_PATH="$GITHUB_WORKSPACE/install/tools/cmake"
          -DGENERIC_CMAKE_MODULE_PATH="$GITHUB_WORKSPACE/install/tools/cmake"
          -DBUILD_TYPE=Debug
          -DBUILD_GMOCK=ON
          -DBUILD_SHARED_LIBS=OFF
          -DCMAKE_POSITION_INDEPENDENT_CODE=ON
          &&
          cmake --build build/googletest -j8
          &&
          cmake --install build/googletest

      - name: Build mocks
        run: >
          cmake
          -S "$GITHUB_WORKSPACE/entservices-testframework/Tests/mocks"
          -B build/mocks
          -DBUILD_SHARED_LIBS=ON
          -DCMAKE_TOOLCHAIN_FILE="${{ env.TOOLCHAIN_FILE }}"
          -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/install/usr"
          -DCMAKE_MODULE_PATH="$GITHUB_WORKSPACE/install/tools/cmake"
          -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}
          -DCMAKE_CXX_FLAGS="
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers
          -I $GITHUB_WORKSPACE/install/usr/include"
          &&
          cmake --build build/mocks -j8
          &&
          cmake --install build/mocks

      - name: Build entservices-deviceanddisplay
        run: >
          cmake
          -S "$GITHUB_WORKSPACE/entservices-deviceanddisplay"
          -B build/entservices-deviceanddisplay
          -DCMAKE_TOOLCHAIN_FILE="${{ env.TOOLCHAIN_FILE }}"
          -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/install/usr"
          -DCMAKE_MODULE_PATH="$GITHUB_WORKSPACE/install/tools/cmake"
          -DHIDE_NON_EXTERNAL_SYMBOLS=OFF
          -DCMAKE_CXX_FLAGS="
          -DEXCEPTIONS_ENABLE=ON
          -fprofile-arcs
          -ftest-coverage
          -DUSE_THUNDER_R4=ON
          -DTHUNDER_VERSION=4
          -DTHUNDER_VERSION_MAJOR=4
          -DTHUNDER_VERSION_MINOR=4
          -DDEVICE_TYPE=AVOutputTV
          -DPLUGIN_PERSISTENTSTORE_PATH="/tmp/secure/persistent/rdkservicestore"
          -DPLUGIN_PERSISTENTSTORE_LEGACYPATH="/tmp/persistent/rdkservicestore"
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/rdk/ds
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/rdk/iarmbus
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/rdk/iarmmgrs-hal
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/systemservices
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/systemservices/proc
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks
          -I $GITHUB_WORKSPACE/install/usr/include
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/devicesettings.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Iarm.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Rfc.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/RBus.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Udev.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Wraps.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/maintenanceMGR.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/pkg.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/secure_wrappermock.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/wpa_ctrl_mock.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/readprocMockInterface.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/btmgr.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tr181api.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvSettings.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvError.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvSettingsExtODM.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvSettingsODM.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvTypes.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/essos-resmgr.h
          -Wall -Wno-unused-result -Wno-deprecated-declarations -Wno-error=format=
          -Wl,-wrap,system -Wl,-wrap,popen -Wl,-wrap,syslog -Wl,-wrap,v_secure_system -Wl,-wrap,v_secure_popen -Wl,-wrap,v_secure_pclose -Wl,-wrap,unlink
          -DUSE_IARMBUS
          -DENABLE_ERM
          -DRDK_LOG_MILESTONE
          -DRDK_SERVICE_L2_TEST
          -DDISABLE_SECURITY_TOKEN
          -DHAS_API_POWERSTATE
          -DENABLE_THERMAL_PROTECTION"
          -DPLUGIN_PERSISTENTSTORE_PATH="/tmp/secure/persistent/rdkservicestore"
          -DPLUGIN_PERSISTENTSTORE_LEGACYPATH="/tmp/persistent/rdkservicestore"
          -DCOMCAST_CONFIG=OFF
          -DCMAKE_DISABLE_FIND_PACKAGE_DS=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_IARMBus=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_Udev=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_RFC=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_RBus=ON
          -DPLUGIN_SYSTEMSERVICES=ON
          -DPLUGIN_POWERMANAGER=ON
          -DPLUGIN_DISPLAYSETTINGS=OFF
          -DPLUGIN_USERPREFERENCES=OFF
          -DPLUGIN_DEVICEDIAGNOSTICS=OFF
          -DPLUGIN_WAREHOUSE=OFF
          -DUSE_THUNDER_R4=ON
          -DPLUGIN_L2Tests=ON
          -DRDK_SERVICE_L2_TEST=ON
          -DDS_FOUND=ON
          -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}
          &&
          cmake --build build/entservices-deviceanddisplay -j8
          &&
          cmake --install build/entservices-deviceanddisplay

      - name: Checkout networkmanager
        uses: actions/checkout@v3
        with:
          repository: rdkcentral/networkmanager
          path: networkmanager
          ref: ${{env.NETWORKMANAGER_REF}}

      - name: Stage INetworkManager.h into install prefix
        run: |
          set -euo pipefail
          dst_dir="$GITHUB_WORKSPACE/install/usr/include/WPEFramework/interfaces"
          mkdir -p "$dst_dir"
          cp -f "$GITHUB_WORKSPACE/networkmanager/interface/INetworkManager.h" "$dst_dir/INetworkManager.h"

      - name: Build entservices-appgateway
        run: >
          cmake
          -S "$GITHUB_WORKSPACE/entservices-appgateway"
          -B build/entservices-appgateway
          -DCMAKE_TOOLCHAIN_FILE="${{ env.TOOLCHAIN_FILE }}"
          -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/install/usr"
          -DCMAKE_MODULE_PATH="$GITHUB_WORKSPACE/install/tools/cmake"
          -DHIDE_NON_EXTERNAL_SYMBOLS=OFF
          -DPLUGIN_CLOUDSTORE_URI="localhost:50051"
          -DCMAKE_CXX_FLAGS="
          -DEXCEPTIONS_ENABLE=ON
          -fprofile-arcs
          -ftest-coverage
          -DTHUNDER_VERSION=4
          -DTHUNDER_VERSION_MAJOR=4
          -DTHUNDER_VERSION_MINOR=4
          -DDEVICE_TYPE=AVOutputTV
          -DPLUGIN_PERSISTENTSTORE_PATH="/tmp/secure/persistent/rdkservicestore"
          -DPLUGIN_PERSISTENTSTORE_LEGACYPATH="/tmp/persistent/rdkservicestore"
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/rdk/ds
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/rdk/iarmbus
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/rdk/iarmmgrs-hal
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/systemservices
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/systemservices/proc
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/libusb
          -I $GITHUB_WORKSPACE/install/usr/include
          -I $GITHUB_WORKSPACE/build/mocks
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/Dobby
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/Dobby/Public/Dobby
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/Dobby/IpcService
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/devicesettings.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Iarm.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Rfc.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/RBus.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Udev.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Wraps.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/libusb/libusb.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/maintenanceMGR.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/pkg.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/secure_wrappermock.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/wpa_ctrl_mock.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/readprocMockInterface.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/btmgr.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tr181api.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvSettings.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvError.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvSettingsExtODM.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvSettingsODM.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvTypes.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/essos-resmgr.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Dobby.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/i_omi_proxy.hpp
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/omi_proxy.hpp
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/Dobby/DobbyProtocol.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/Dobby/DobbyProxy.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/Dobby/Public/Dobby/IDobbyProxy.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/Dobby/IpcService/IpcFactory.h
          -Werror -Wall -Wno-unused-result -Wno-unused-variable -Wno-deprecated-declarations -Wno-error=format= -Wno-error=format-truncation
          -DUSE_IARMBUS
          -DHAS_RBUS
          -DRDK_SERVICE_L2_TEST
          -DDISABLE_SECURITY_TOKEN
          -DENABLE_THERMAL_PROTECTION"
          -DPLUGIN_PERSISTENTSTORE_PATH="/tmp/secure/persistent/rdkservicestore"
          -DPLUGIN_PERSISTENTSTORE_LEGACYPATH="/tmp/persistent/rdkservicestore"
          -DPLUGIN_STORAGE_MANAGER_PATH="/tmp/persistent/storageManager"
          -DCOMCAST_CONFIG=OFF
          -DCMAKE_DISABLE_FIND_PACKAGE_DS=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_IARMBus=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_Udev=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_RFC=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_RBus=ON
          -DPLUGIN_MIGRATION=ON
          -DPLUGIN_TELEMETRY=ON
          -DPLUGIN_USERSETTINGS=ON
          -DPLUGIN_PERSISTENTSTORE=ON
          -DPLUGIN_SHAREDSTORAGE=ON
          -DPLUGIN_RESOURCEMANAGER=ON
          -DPLUGIN_CLOUDSTORE=ON
          -DPLUGIN_USBDEVICE=ON
          -DPLUGIN_USB_MASS_STORAGE=ON
          -DPLUGIN_LIFECYCLEMANAGER=OFF
          -DPLUGIN_APPMANAGER=OFF
          -DPLUGIN_ANALYTICS=ON
          -DPLUGIN_ANALYTICS_BACKEND_LIBRARY_NAME="libWPEFrameworkAnalyticsBackendMock.so"
          -DPLUGIN_ANALYTICS_EVENTS_MAP="/tmp/AnalyticsEventsMap.json"
          -DUSE_THUNDER_R4=ON
          -DPLUGIN_L2Tests=ON
          -DRDK_SERVICE_L2_TEST=ON
          -DPLUGIN_STORAGE_MANAGER=ON
          -DPLUGIN_OCICONTAINER=ON
          -DPLUGIN_APPGATEWAY=ON
          -DPLUGIN_APPGATEWAYCOMMON=ON
          -DPLUGIN_APPNOTIFICATIONS=ON
          -DPLUGIN_IMPLEMENTATION=WPEFrameworkAppGateway
          -DPLUGIN_APPGATEWAY_MODE=Off
          -DPLUGIN_APPGATEWAYCOMMON_MODE=Off
          -DDS_FOUND=ON
          -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}
          &&
          cmake --build build/entservices-appgateway -j6
          &&
          sudo mkdir -p /etc/app-gateway
          &&
          sudo chmod 777 /etc/app-gateway
          &&
          cmake --install build/entservices-appgateway

      - name: Build entservices-testframework
        run: >
          cmake
          -S "$GITHUB_WORKSPACE/entservices-testframework"
          -B build/entservices-testframework
          -DCMAKE_TOOLCHAIN_FILE="${{ env.TOOLCHAIN_FILE }}"
          -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/install/usr"
          -DCMAKE_MODULE_PATH="$GITHUB_WORKSPACE/install/tools/cmake"
          -DHIDE_NON_EXTERNAL_SYMBOLS=OFF
          -DCMAKE_CXX_FLAGS="
          -DEXCEPTIONS_ENABLE=ON
          -fprofile-arcs
          -ftest-coverage
          -DUSE_THUNDER_R4=ON
          -DTHUNDER_VERSION=4
          -DTHUNDER_VERSION_MAJOR=4
          -DTHUNDER_VERSION_MINOR=4
          -DDEVICE_TYPE=AVOutputTV
          -DPLUGIN_PERSISTENTSTORE_PATH="/tmp/secure/persistent/rdkservicestore"
          -DPLUGIN_PERSISTENTSTORE_LEGACYPATH="/tmp/persistent/rdkservicestore"
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/rdk/ds
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/rdk/iarmbus
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/rdk/iarmmgrs-hal
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/systemservices
          -I $GITHUB_WORKSPACE/entservices-testframework/Tests/headers/systemservices/proc
          -I $GITHUB_WORKSPACE/entservices-deviceanddisplay/helpers
          -I $GITHUB_WORKSPACE/entservices-appgateway/helpers
          -I $GITHUB_WORKSPACE/install/usr/include
          -I ./usr/include/libdrm
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/devicesettings.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Iarm.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Rfc.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/RBus.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Udev.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/Wraps.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/maintenanceMGR.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/pkg.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/secure_wrappermock.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/wpa_ctrl_mock.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/readprocMockInterface.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/btmgr.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tr181api.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvSettings.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvError.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvSettingsExtODM.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvSettingsODM.h
          -include $GITHUB_WORKSPACE/entservices-testframework/Tests/mocks/tvTypes.h
          -Werror -Wall -Wno-unused-result -Wno-deprecated-declarations -Wno-error=format=
          -Wl,--no-as-needed
          -DUSE_IARMBUS
          -DRDK_SERVICE_L2_TEST
          -DDISABLE_SECURITY_TOKEN
          -DENABLE_THERMAL_PROTECTION"
          -DPLUGIN_PERSISTENTSTORE_PATH="/tmp/secure/persistent/rdkservicestore"
          -DPLUGIN_PERSISTENTSTORE_LEGACYPATH="/tmp/persistent/rdkservicestore"
          -DCOMCAST_CONFIG=OFF
          -DCMAKE_DISABLE_FIND_PACKAGE_DS=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_IARMBus=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_Udev=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_RFC=ON
          -DCMAKE_DISABLE_FIND_PACKAGE_RBus=ON
          -DPLUGIN_TELEMETRY=ON
          -DPLUGIN_USERSETTINGS=ON
          -DPLUGIN_PERSISTENTSTORE=ON
          -DPLUGIN_RESOURCEMANAGER=ON
          -DPLUGIN_SHAREDSTORAGE=ON
          -DPLUGIN_CLOUDSTORE=ON
          -DPLUGIN_USBDEVICE=ON
          -DPLUGIN_USB_MASS_STORAGE=ON
          -DPLUGIN_ANALYTICS=ON
          -DPLUGIN_ANALYTICS_BACKEND_LIBRARY_NAME="libWPEFrameworkAnalyticsBackendMock.so"
          -DPLUGIN_ANALYTICS_EVENTS_MAP="/tmp/AnalyticsEventsMap.json"
          -DPLUGIN_LIFECYCLEMANAGER=OFF
          -DPLUGIN_APPMANAGER=OFF
          -DPLUGIN_AVINPUT=OFF
          -DPLUGIN_AVOUTPUT=OFF
          -DAVOUTPUT_TV=OFF
          -DPLUGIN_STORAGE_MANAGER=ON
          -DUSE_THUNDER_R4=ON
          -DPLUGIN_L2Tests=ON
          -DRDK_SERVICE_L2_TEST=ON
          -DDS_FOUND=ON
          -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}
          &&
          cmake --build build/entservices-testframework -j8
          &&
          cmake --install build/entservices-testframework

      - name: Set up files
        run: >
          sudo mkdir -p -m 777
          /opt/persistent
          /opt/secure
          /opt/secure/reboot
          /opt/secure/persistent
          /opt/secure/persistent/System
          /tmp/secure/persistent
          /tmp/persistent
          /tmp/persistent/rdkservicestore
          /opt/logs
          /lib/rdk
          /run/media/sda1/logs/PreviousLogs
          /run/media/sda2/logs/PreviousLogs
          /run/sda1/UsbTestFWUpdate
          /run/sda1/UsbProdFWUpdate
          /run/media/sda1/Logs
          /run/sda2
          /tmp/bus/usb/devices/100-123
          /tmp/bus/usb/devices/101-124
          /tmp/block/sda/device
          /tmp/block/sdb/device
          /dev/disk/by-id
          /dev &&
          if [ ! -f mknod /dev/sda c 240 0 ]; then mknod /dev/sda c 240 0; fi &&
          if [ ! -f mknod /dev/sda1 c 240 0 ]; then mknod /dev/sda1 c 240 0; fi &&
          if [ ! -f mknod /dev/sda2 c 240 0 ]; then mknod /dev/sda2 c 240 0; fi &&
          if [ ! -f mknod /dev/sdb c 240 0 ]; then mknod /dev/sdb c 240 0; fi &&
          if [ ! -f mknod /dev/sdb1 c 240 0 ]; then mknod /dev/sdb1 c 240 0; fi &&
          if [ ! -f mknod /dev/sdb2 c 240 0 ]; then mknod /dev/sdb2 c 240 0; fi
          &&
          sudo touch
          /opt/standbyReason.txt
          /opt/tmtryoptout
          /opt/fwdnldstatus.txt
          /opt/dcm.properties
          /etc/device.properties
          /etc/dcm.properties
          /etc/authService.conf
          /version.txt
          /run/media/sda1/logs/PreviousLogs/logFile.txt
          /run/media/sda1/logs/test.txt
          /run/media/sda1/logs/test.png
          /run/media/sda1/logs/test.docx
          /run/media/sda2/logs/test.txt
          /run/media/sda2/logs/test.png
          /run/media/sda1/Logs/5C3400F15492_Logs_12-05-22-10-41PM.tgz
          /run/sda1/HSTP11MWR_5.11p5s1_VBN_sdy.bin
          /run/sda1/UsbTestFWUpdate/HSTP11MWR_3.11p5s1_VBN_sdy.bin
          /run/sda1/UsbProdFWUpdate/HSTP11MWR_4.11p5s1_VBN_sdy.bin
          /lib/rdk/getMaintenanceStartTime.sh
          /tmp/opkg.conf
          /tmp/system_service_temp.conf
          /tmp/secure/persistent/rdkservicestore
          /tmp/bus/usb/devices/100-123/serial
          /tmp/bus/usb/devices/101-124/serial
          /tmp/block/sda/device/vendor
          /tmp/block/sda/device/model
          /tmp/block/sdb/device/vendor
          /tmp/block/sdb/device/model
          &&
          sudo chmod -R 777
          /opt/standbyReason.txt
          /opt/tmtryoptout
          /opt/fwdnldstatus.txt
          /opt/dcm.properties
          /etc/device.properties
          /etc/dcm.properties
          /etc/authService.conf
          /version.txt
          /lib/rdk/getMaintenanceStartTime.sh
          /tmp/opkg.conf
          /tmp/system_service_temp.conf
          /tmp/persistent/rdkservicestore
          /tmp/secure/persistent/rdkservicestore
          /tmp/bus/usb/devices/100-123/serial
          /tmp/block/sda/device/vendor
          /tmp/block/sda/device/model
          /tmp/bus/usb/devices/101-124/serial
          /tmp/block/sdb/device/vendor
          /tmp/block/sdb/device/model
          &&
          cd /dev/disk/by-id/
          &&
          sudo ln -s ../../sda /dev/disk/by-id/usb-Generic_Flash_Disk_B32FD507-0
          &&
          sudo ln -s ../../sdb /dev/disk/by-id/usb-JetFlash_Transcend_16GB_UEUIRCXT-0
          &&
          ls -l /dev/disk/by-id/usb-Generic_Flash_Disk_B32FD507-0
          &&
          ls -l /dev/disk/by-id/usb-JetFlash_Transcend_16GB_UEUIRCXT-0

      - name: Download pact_verifier_cli
        run: |
          export PATH="$GITHUB_WORKSPACE/install/usr/bin:${PATH}"
          $GITHUB_WORKSPACE/entservices-testframework/Tests/L2Tests/pact/install-verifier-cli.sh

      - name: Build gcov SIGSEGV flush helper
        if: ${{ matrix.coverage == 'with-coverage' && !env.ACT }}
        run: |
          cat > /tmp/gcov_segv_flush.c << 'CEOF'
          #define _GNU_SOURCE
          #include <dlfcn.h>
          #include <link.h>
          #include <signal.h>
          #include <stdio.h>
          #include <string.h>
          #include <stdint.h>
          #include <elf.h>
          #include <pthread.h>
          #include <unistd.h>
          #include <fcntl.h>
          #include <stdlib.h>
          #include <sys/types.h>

          /*
           * Strategy: background thread pre-caches gcov flush pointers every 2s.
           * Signal handler reads only from the pre-built cache — no dl locking.
           *
           * Key insight: __gcov_exit (from libgcov.a _gcov.o) is ALWAYS linked in
           * via __gcov_init → atexit(__gcov_exit).  However it may have STV_HIDDEN
           * visibility and therefore NOT appear in .dynsym.
           *
           * Solution: scan the .so FILE ON DISK via its .symtab section (not
           * .dynsym).  .symtab contains ALL symbols regardless of visibility.
           * This is safe to do in a background thread (file I/O + malloc are OK).
           *
           * sigaction() interposer keeps our handler installed even after
           * WPEFramework's main() calls sigaction(SIGSEGV, ...) to replace it.
           */

          typedef void (*gcov_fn_t)(void);
          typedef int  (*real_sigaction_fn_t)(int, const struct sigaction *, struct sigaction *);

          static real_sigaction_fn_t s_real_sigaction = NULL;
          static struct sigaction s_chained_sa;
          static volatile int s_flushed = 0;

          /* Pre-cached gcov flush pointers — written by thread, read by signal handler */
          #define MAX_GCOV_ENTRIES 512
          static gcov_fn_t s_gcov_cache[MAX_GCOV_ENTRIES];
          static volatile int s_gcov_cache_count = 0;

          #define IS_GCOV_FN(n) \
              (strcmp((n), "__gcov_exit")  == 0 || \
               strcmp((n), "__gcov_dump")  == 0 || \
               strcmp((n), "__gcov_flush") == 0)

          /*
           * Disk-based ELF scanner: reads .symtab from the .so file on disk.
           * Finds __gcov_exit even when it is STV_HIDDEN (absent from .dynsym).
           * Uses pread + malloc — safe in a regular thread, NOT for signal context.
           */
          static gcov_fn_t find_gcov_fn_disk(struct dl_phdr_info *info)
          {
              if (!info->dlpi_name || info->dlpi_name[0] == '\0') return NULL;

              gcov_fn_t   result   = NULL;
              char       *shdrs_buf = NULL;
              char       *shnames   = NULL;
              char       *strtab    = NULL;
              char       *syms_buf  = NULL;

              int fd = open(info->dlpi_name, O_RDONLY);
              if (fd < 0) return NULL;

              ElfW(Ehdr) ehdr;
              if (pread(fd, &ehdr, sizeof(ehdr), 0) != (ssize_t)sizeof(ehdr)) goto done;
              /* Basic magic check */
              if (ehdr.e_ident[0] != 0x7f || ehdr.e_ident[1] != 'E' ||
                  ehdr.e_ident[2] != 'L'  || ehdr.e_ident[3] != 'F') goto done;
              if (ehdr.e_shoff == 0 || ehdr.e_shnum == 0) goto done; /* stripped */
              if (ehdr.e_shentsize != sizeof(ElfW(Shdr)))            goto done;

              /* Read all section headers */
              shdrs_buf = (char *)malloc((size_t)ehdr.e_shnum * sizeof(ElfW(Shdr)));
              if (!shdrs_buf) goto done;
              if (pread(fd, shdrs_buf, (size_t)ehdr.e_shnum * sizeof(ElfW(Shdr)),
                        (off_t)ehdr.e_shoff)
                      != (ssize_t)((size_t)ehdr.e_shnum * sizeof(ElfW(Shdr)))) goto done;
              ElfW(Shdr) *shdrs = (ElfW(Shdr) *)shdrs_buf;

              /* Read .shstrtab so we can compare section names (optional but tidy) */
              if (ehdr.e_shstrndx == 0 || ehdr.e_shstrndx >= (uint16_t)ehdr.e_shnum) goto done;
              {
                  ElfW(Shdr) *shstrtab_sh = &shdrs[ehdr.e_shstrndx];
                  shnames = (char *)malloc(shstrtab_sh->sh_size + 1);
                  if (!shnames) goto done;
                  if (pread(fd, shnames, shstrtab_sh->sh_size,
                            (off_t)shstrtab_sh->sh_offset)
                          != (ssize_t)shstrtab_sh->sh_size) goto done;
                  shnames[shstrtab_sh->sh_size] = '\0';
              }

              /* Locate SHT_SYMTAB section (there is at most one per .so) */
              ElfW(Shdr) *sym_sh = NULL;
              for (int i = 0; i < (int)ehdr.e_shnum; i++) {
                  if (shdrs[i].sh_type == SHT_SYMTAB) { sym_sh = &shdrs[i]; break; }
              }
              if (!sym_sh || sym_sh->sh_size == 0) goto done; /* stripped */
              if (sym_sh->sh_link == 0 || sym_sh->sh_link >= (uint32_t)ehdr.e_shnum) goto done;

              /* .strtab for symbol names is pointed to by sym_sh->sh_link */
              {
                  ElfW(Shdr) *str_sh = &shdrs[sym_sh->sh_link];
                  strtab = (char *)malloc(str_sh->sh_size + 1);
                  if (!strtab) goto done;
                  if (pread(fd, strtab, str_sh->sh_size, (off_t)str_sh->sh_offset)
                          != (ssize_t)str_sh->sh_size) goto done;
                  strtab[str_sh->sh_size] = '\0';
              }

              /* Read entire .symtab and search for a gcov function */
              {
                  size_t entsize = sym_sh->sh_entsize ? sym_sh->sh_entsize : sizeof(ElfW(Sym));
                  size_t nsyms   = sym_sh->sh_size / entsize;
                  syms_buf = (char *)malloc(sym_sh->sh_size);
                  if (!syms_buf) goto done;
                  if (pread(fd, syms_buf, sym_sh->sh_size, (off_t)sym_sh->sh_offset)
                          != (ssize_t)sym_sh->sh_size) goto done;

                  ElfW(Shdr) *str_sh = &shdrs[sym_sh->sh_link];
                  for (size_t i = 0; i < nsyms && !result; i++) {
                      ElfW(Sym) *sym = (ElfW(Sym) *)(syms_buf + i * entsize);
                      if (!sym->st_value) continue;
                      if ((sym->st_info & 0xf) != STT_FUNC) continue;
                      if (sym->st_name >= (ElfW(Word))str_sh->sh_size) continue;
                      const char *n = strtab + sym->st_name;
                      if (IS_GCOV_FN(n)) {
                          result = (gcov_fn_t)(info->dlpi_addr + sym->st_value);
                          fprintf(stderr, "[gcov_segv] found %s in %s → %p\n",
                                  n, info->dlpi_name, (void *)result);
                      }
                  }
              }

          done:
              free(syms_buf);
              free(strtab);
              free(shnames);
              free(shdrs_buf);
              close(fd);
              return result;
          }

          typedef struct { int count; gcov_fn_t *arr; } CacheCtx;

          static int cache_one(struct dl_phdr_info *info, size_t sz, void *data) {
              (void)sz;
              CacheCtx *ctx = (CacheCtx *)data;
              if (ctx->count >= MAX_GCOV_ENTRIES) return 1;
              if (!info->dlpi_name || !info->dlpi_name[0]) return 0;
              gcov_fn_t fn = find_gcov_fn_disk(info);
              if (fn) ctx->arr[ctx->count++] = fn;
              return 0;
          }

          /*
           * Background thread: calls dl_iterate_phdr (safe here — not signal context),
           * then disk-scans each mapped .so to find __gcov_exit/__gcov_dump.
           * Rescans every 2s to pick up plugins loaded by test cases (autostart=false).
           * Signal handler reads only the pre-built cache — no locking needed there.
           */
          static void *cache_thread(void *arg) {
              (void)arg;
              fprintf(stderr, "[gcov_segv] cache thread started\n");
              for (;;) {
                  gcov_fn_t tmp[MAX_GCOV_ENTRIES];
                  CacheCtx ctx = { 0, tmp };
                  dl_iterate_phdr(cache_one, &ctx);
                  fprintf(stderr, "[gcov_segv] cache scan: %d gcov entries found\n", ctx.count);
                  if (ctx.count > 0) {
                      memcpy((void *)s_gcov_cache, tmp, (size_t)ctx.count * sizeof(gcov_fn_t));
                      __sync_synchronize();   /* store-store barrier before publishing count */
                      s_gcov_cache_count = ctx.count;
                  }
                  sleep(2);
              }
              return NULL;
          }

          /* ---- Signal handler: only reads pre-built cache, no dl/malloc calls ---- */
          static void segv_handler(int sig, siginfo_t *si, void *ctx) {
              if (!s_flushed) {
                  s_flushed = 1;
                  int count = s_gcov_cache_count;
                  fprintf(stderr, "[gcov_segv] SIGSEGV caught — calling %d cached gcov fn(s)\n", count);
                  for (int i = 0; i < count; i++) {
                      if (s_gcov_cache[i]) s_gcov_cache[i]();
                  }
                  fprintf(stderr, "[gcov_segv] flush done — chaining to Thunder handler\n");
              }
              /* Chain to WPEFramework's SIGSEGV handler */
              if (s_chained_sa.sa_flags & SA_SIGINFO) {
                  if (s_chained_sa.sa_sigaction)
                      s_chained_sa.sa_sigaction(sig, si, ctx);
              } else if (s_chained_sa.sa_handler &&
                         s_chained_sa.sa_handler != SIG_DFL &&
                         s_chained_sa.sa_handler != SIG_IGN) {
                  s_chained_sa.sa_handler(sig);
              } else {
                  struct sigaction dfl;
                  memset(&dfl, 0, sizeof(dfl));
                  dfl.sa_handler = SIG_DFL;
                  s_real_sigaction(SIGSEGV, &dfl, NULL);
                  raise(sig);
              }
          }

          /* sigaction interposer: intercept WPEFramework's sigaction(SIGSEGV,...) call */
          int sigaction(int sig, const struct sigaction *act, struct sigaction *oldact) {
              if (!s_real_sigaction)
                  s_real_sigaction = (real_sigaction_fn_t)(uintptr_t)dlsym(RTLD_NEXT, "sigaction");
              if (sig == SIGSEGV && act != NULL) {
                  if (oldact) *oldact = s_chained_sa;
                  s_chained_sa = *act; /* save Thunder's handler as chain target */
                  return 0;           /* keep our handler installed */
              }
              return s_real_sigaction(sig, act, oldact);
          }

          __attribute__((constructor))
          static void install_handler(void) {
              s_real_sigaction = (real_sigaction_fn_t)(uintptr_t)dlsym(RTLD_NEXT, "sigaction");
              struct sigaction sa;
              memset(&sa, 0, sizeof(sa));
              sa.sa_sigaction = segv_handler;
              sa.sa_flags = SA_SIGINFO;
              s_real_sigaction(SIGSEGV, &sa, &s_chained_sa);

              /* Launch background cache thread — scans immediately, refreshes every 2s */
              pthread_t tid;
              pthread_attr_t attr;
              pthread_attr_init(&attr);
              pthread_attr_setdetachstate(&attr, PTHREAD_CREATE_DETACHED);
              pthread_create(&tid, &attr, cache_thread, NULL);
              pthread_attr_destroy(&attr);

              fprintf(stderr, "[gcov_segv] handler installed, cache thread launched\n");
          }
          CEOF
          gcc -O0 -shared -fPIC -o /tmp/libgcov_segv_flush.so /tmp/gcov_segv_flush.c -ldl -lpthread
          echo "Built /tmp/libgcov_segv_flush.so"

      - name: Run unit tests without valgrind
        run: |
          export PATH=$GITHUB_WORKSPACE/install/usr/bin:${PATH}
          export LD_LIBRARY_PATH=$GITHUB_WORKSPACE/install/usr/lib:$GITHUB_WORKSPACE/install/usr/lib/wpeframework/plugins:${LD_LIBRARY_PATH}
          # For coverage builds, wrap the WPEFramework binary with a shell script
          # that forces LD_PRELOAD into the WPEFramework process itself.
          # RdkServicesL2Test clears its env when spawning WPEFramework, so setting
          # LD_PRELOAD in the parent shell is not enough — the wrapper ensures it.
          WPE_BIN=$GITHUB_WORKSPACE/install/usr/bin/WPEFramework
          # Resolve symlink so we wrap the actual ELF binary, not the symlink itself
          WPE_REAL=$(readlink -f "$WPE_BIN" 2>/dev/null || echo "$WPE_BIN")
          if [ -f /tmp/libgcov_segv_flush.so ] && [ -f "$WPE_REAL" ] && [ ! -f "${WPE_REAL}.real" ]; then
            mv "$WPE_REAL" "${WPE_REAL}.real"
            printf '#!/bin/sh\nexport LD_PRELOAD=/tmp/libgcov_segv_flush.so${LD_PRELOAD:+:$LD_PRELOAD}\nexec "%s.real" "$@"\n' "$WPE_REAL" > "$WPE_REAL"
            chmod +x "$WPE_REAL"
            echo "Wrapped WPEFramework with gcov SIGSEGV handler (real: $WPE_REAL)"
          fi
          set +e
          RdkServicesL2Test
          rc=$?
          set -e
          # Restore original binary
          if [ -f "${WPE_REAL}.real" ]; then
            mv "${WPE_REAL}.real" "$WPE_REAL"
          fi
          # Thunder may exit with SIGSEGV on shutdown; treat only that as non-fatal.
          if [ $rc -ne 0 ] && [ $rc -ne 139 ]; then
            exit $rc
          fi
          cp -f $(pwd)/rdkL2TestResults.json $GITHUB_WORKSPACE/rdkL2TestResultsWithoutValgrind.json 2>/dev/null || true
          rm -f $(pwd)/rdkL2TestResults.json 2>/dev/null || true

      - name: Generate coverage
        if: ${{ matrix.coverage == 'with-coverage' && !env.ACT }}
        run: >
          cp $GITHUB_WORKSPACE/entservices-testframework/Tests/L2Tests/.lcovrc_l2 ~/.lcovrc
          &&
          lcov -c
          -o coverage.info
          -d build/entservices-appgateway
          &&
          lcov
          -r coverage.info
          '/usr/include/*'
          '*/build/entservices-appgateway/_deps/*'
          '*/build/entservices-deviceanddisplay/_deps/*'
          '*/build/entservices-entservices-testframework/_deps/*'
          '*/build/mocks/*'
          '*/install/usr/include/*'
          '*/Tests/headers/*'
          '*/Tests/mocks/*'
          '*/Tests/L2Tests/*'
          '*/sqlite/*'
          -o filtered_coverage.info
          &&
          genhtml
          -o coverage
          -t "entservices-appgateway coverage"
          filtered_coverage.info

      - name: Run unit tests with valgrind
        if: ${{ !env.ACT }}
        run: >
          PATH=$GITHUB_WORKSPACE/install/usr/bin:${PATH}
          LD_LIBRARY_PATH=$GITHUB_WORKSPACE/install/usr/lib:$GITHUB_WORKSPACE/install/usr/lib/wpeframework/plugins:${LD_LIBRARY_PATH}
          valgrind
          --tool=memcheck
          --log-file=valgrind_log
          --leak-check=yes
          --show-reachable=yes
          --track-fds=yes
          --fair-sched=try
          RdkServicesL2Test &&
          cp -rf $(pwd)/rdkL2TestResults.json $GITHUB_WORKSPACE/rdkL2TestResultsWithValgrind.json &&
          rm -rf $(pwd)/rdkL2TestResults.json

      - name: Upload artifacts
        if: ${{ !env.ACT }}
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-L2-infra
          path: |
            coverage/
            valgrind_log
            rdkL2TestResultsWithoutValgrind.json
            rdkL2TestResultsWithValgrind.json
          if-no-files-found: warn
